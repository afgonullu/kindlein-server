export const BaseDefs = `#graphql
  type Query {
    _empty: String
  }
  type Mutation {
    _empty: String
  }
  type Subscription {
    _empty: String
  }

  type User {
    _empty: String
  }

  type Child {
    _empty: String
  }

  type Moment {
    _empty: String
  }
`;
